table.table.table-hover.table-bordered.item-extras
   thead
      tr
         th
         each getItemHeaders
            th
               img(src='{{this}}')
   tbody
      each getItemData
         tr
            td(style='text-align:right; padding-right: 10px;font-weight: bold') {{field2label label}}
            each data
               td {{this}}

